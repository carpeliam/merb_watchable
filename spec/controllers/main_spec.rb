require File.dirname(__FILE__) + '/../spec_helper'

# No controllers in this slice.
describe "MerbWatchable::Main (controller)" do
  # I'd get rid of this if I could, but that seems to have adverse effects.
  it "should be empty" do; end
end
